# v0.10.2 "trust boundary + operational honesty" — implementation plan

**Goal:** Close the two code sites where a peer's bytes gain authority (owed ledger, terminal), plus four operational-honesty fixes surfaced by tonight's live incidents. Strict PATCH: fixes/docs/test-hardening only, no CLI removals/renames.

**Source:** deep-analysis-v2 (N1-N8) + coordinator addendum (M1-M4), all adversarially verified against origin/main @ 62e364a by a 4-way review (claude/codex/sonnet/gemini).

## Global Constraints
- Patch discipline (CONTRIBUTING.md:88): no CLI command/flag removals or renames; fixes/docs/tests only.
- All work branches off origin/main in a detached worktree; NEVER the shared live tree.
- Crown jewels: `TestPromptInjectionBytes*` must pass byte-identically on every PR.
- Every PR: gofmt/vet/build + `go test ./...` + `-race` + conformance module + drift test, verified by the developer before review.
- Gate loop: 1 develops, 2 seniors review until all happy; I merge on green + Alex-authorized tag.

## Agreed fix list (4-way)

| ID | Fix | Severity | Notes |
|----|-----|----------|-------|
| N1 | `displayConsumed` discharge requires `msg.Sender == key.To` | Medium | in-scope compromised-peer mechanism (§15); impact bounded to a non-blocking gate warning. Single call site covers all discharge paths. |
| N6a | regression test: reply from non-owing sender does NOT discharge | — | ships in the N1 commit (CLI test; conformance Binding has no owed ledger) |
| N3 | sanitize terminal control bytes from consumed output | Med-High | FORK to settle (see Task 2) |
| M1 | doctor: on-disk AGENTCHUTE.md vs embedded spec hash → WARN on mismatch | Medium | prevents tonight's split-brain; compare vs `embeddedSpecContent`, NOT a line-3 token |
| M2 | doctor `unenrolled_presence`: drop or demote-to-info | Medium | vestige of pre-pull-only wake era; false-positive in every tmux fleet (design call: Task 4) |
| M3 | docs: hand-protocol scoping covenant + README.md:119 + wrapper §5→Appendix C | Medium | docs-only; generalizes AGENTS.md:77 |
| M4 | runner: re-check inbox non-empty before `injectPrompt` | Low | kills spurious-cue turn waste |
| N7 | spec sentence: `.live` freshness = supervisor tick, not agent processing | Low | serve.go:559 heartbeats `.live` unconditionally |
| §15 | one sentence naming N1/N3 as the now-enforced mechanisms | — | codex's suggestion |

**Optional (include only if one-liners):** N4 doctor/pending inbox-flood threshold warn (depth already visible — NOT the `--limit` default change); N2 doc comment on `ParseMessageFrontmatter` last-wins.

**Deferred → 0.11.0 / additions round:** N4 `--limit` default; N6b conformance module rename off `spike` + language-neutral vectors + second-language impl; N8 stable internal idempotency keys (+retry); F2 `v:2`; `--idempotency-key`; `AGENTCHUTE_LEASE_TIMEOUT`; IsLive mtime; HUMAN.md; cue-channel ladder.

**Verified-safe, do NOT touch:** N5 (archive dedup is `os.SameFile` inode identity; seq non-reuse makes filenames unique).

---

## Task 1 — N1 owed-discharge sender guard + N6a test  (dev: codex; gate: sonnet + claude)
**Files:** `internal/cli/check.go` (~259 guard), `internal/loop/owed.go` (leave `ClearOwed` sig; guard at call site), `AGENTCHUTE.md` §6.4 (clarifying note), new/existing CLI test (`internal/cli/consume_boundary_test.go`).
- Add `msg.Sender == key.To` to the discharge guard in `displayConsumed` (msg.Sender is filename-derived, authoritative per check.go:186; covers fresh + redelivered paths — single prod call site).
- N6a: test that Carol's reply carrying Alice→Bob's ref does NOT clear Alice's owed entry to Bob; keep the correct-sender positive path green.
- §6.4: clarify discharge is asker-local and requires the reply come from the owing agent. **Covenant note:** `.owed` is asker-local, non-wire, "warnings never blockers" (owed.go:18) — tightening when a local ledger clears does not change interop/conformance; codex to confirm this framing is patch-safe (flag for Alex if not).
**Acceptance:** forged-third-party reply leaves ledger untouched (test); correct-sender discharge still works; crown jewels green.

## Task 2 — N3 terminal-control sanitization  (dev: sonnet; gate: codex + claude)
**Files:** `internal/cli/check.go` (~282 printConsumedBody), `internal/cli/pending.go` (~225 raw frontmatter text mode), new test.
- **FORK to settle (dev picks, reviewers sign off):** (a) strip C0/C1 unconditionally keep `\n\t` (sonnet: bodies are spec'd UTF-8 text; simplest; no new stdout-TTY detection) vs (b) TTY-gated, raw when piped (codex/doc: preserves exact bytes downstream). Coordinator lean: (a).
- Test: an ANSI/OSC/C0-laden body prints inert; the runner cue path stays body-free (already verified).
**Acceptance:** control bytes render inert through the display path; `--show-body` JSON path unchanged (already json-escaped); crown jewels green.

## Task 3 — M1 doctor spec-staleness check  (dev: codex; gate: sonnet + claude)
**Files:** `internal/cli/doctor.go` (new check), uses `embeddedSpecContent`.
- New doctor check: hash/byte-compare on-disk `AGENTCHUTE.md` against `embeddedSpecContent`; WARN (not block) on mismatch with a "re-run `agentchute init`/`setup` or update your checkout" hint. Do NOT compare against a version token (line 3 lags the binary on every patch).
**Acceptance:** a stale on-disk spec produces a WARN; an in-sync spec is OK; test both.

## Task 4 — M2 doctor unenrolled_presence  (dev: codex, bundled with Task 3 or its own PR; gate: sonnet + claude)
**Files:** `internal/cli/presence_scan.go` (~226), `internal/cli/doctor.go` (~477), test.
- **Design call (reviewers ratify):** demote the tmux-pane branch to informational, OR remove it. Coordinator lean: remove — in pull-only there is no pane→registration correlation for it to assert, so it only emits noise. Keep any genuinely-useful part (a registration with a stale `.live` is already covered elsewhere — verify no coverage is lost).
**Acceptance:** a healthy tmux fleet produces no false `unenrolled_presence` warning; test.

## Task 5 — M4 runner cue re-check  (dev: sonnet; gate: codex + claude)
**Files:** `internal/cli/serve.go` (~injectLoop ~623), test.
- Before `injectPrompt`, re-check the inbox is non-empty; skip the injection if it emptied since the wake was enqueued (claim race). Preserve the safe failure direction (never miss mail).
**Acceptance:** a wake whose mail was already claimed injects no cue; a wake with pending mail still injects; test the race.

## Task 6 — docs bundle  (author: gemini; plumb: claude; gate: codex + sonnet)
**Files:** `AGENTCHUTE.md` (§15 sentence for N1/N3; §9/§10 sentence for N7; hand-protocol scoping covenant in §5/Appendix C for M3), `README.md:119`, wrapper files + templates (§5→Appendix C pointer, M3 covenant).
- M3: covenant "an agent with the reference CLI available MUST use it; hand-protocol is for no-binary environments only"; fix README.md:119; fix wrapper §5→Appendix C pointers.
- N7: `.live` freshness = supervisor heartbeat tick, not agent processing.
- §15: name N1/N3 as now-enforced mechanisms.
- **Marker:** if the wrapper enrollment marked-block text changes (the §5→Appendix C pointer is inside it), bump enrollmentVersion v22→v23 + re-stamp + drift test. If M3's covenant lives outside the marked block, no bump.
**Acceptance:** drift test green; README/wrapper pointers correct; no spec renumbering (additions only, at section ends).

## Task 7 — release-prep  (dev: claude; gate: codex + sonnet)
**Files:** `CHANGELOG.md`, `docs/internal/HANDOFF.md`, optional N4 threshold if it stayed a one-liner.
- CHANGELOG v0.10.2 entry (attribution-accurate per PR — the lesson from v0.10.1 #44); HANDOFF pointer.
**Acceptance:** entry matches merged commits; RC preflight green; tag v0.10.2 → goreleaser → asset smoke.

## Self-review notes
- Every fix maps to a verified finding; no placeholders.
- N1's `ClearOwed` signature is left unchanged (guard at call site) to avoid a wider blast radius; if reviewers prefer threading sender into `ClearOwed` for defense-in-depth, that's a Task 1 sub-decision.
- Task 3+4 may merge into one doctor PR if small; kept separate here for independent review.
