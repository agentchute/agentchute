# v0.1.1 HANDOFF (claude-code queue complete; awaiting Alex's merge approval)

Snapshot at end of claude-code's session, 2026-05-19. Branch `v0.1.1` is alive on `origin`. Resume agents: read this before doing anything else on v0.1.1 work.

## Completion status (2026-05-19, post-session)

Phase 1 + Phase 2 are **complete**, including post-real-bake fixups from the multi-agent review loop. All 20 commits land on `v0.1.1` (origin synced). Phase 3 polish (`watch`, `doctor`, `--json` everywhere) deferred to v0.1.2 per handoff scoping.

Commits since `f984c84` (session-pause snapshot):

```
f95ea55 send.go: self-send-ask warning + reply_required propagation invariant tests
05e269d docs: add loop prevention convention and fix Claude hook template          (gemini)
52fbef4 docs: finalize v0.1.1 changelog and protocol task list                     (gemini)
5460f50 pending.go: lenient frontmatter delimiters in the peek path
0d468fa check.go + V0.1.1-HANDOFF.md: codex final-pass review (5320c08)
5320c08 V0.1.1-HANDOFF.md: claude-code queue complete; awaiting Alex's merge approval
11742d2 pending_test: spec rev3 Test 2 + Test 7 read-only invariant
b410a23 check.go: record pending replies on archive of reply_required messages
b744c31 .claude/settings.json template: SessionStart + UserPromptSubmit + Stop hooks
aa5f0d9 send.go: address codex review on 89ad2d9 (mismatched --reply-to + line-scoped frontmatter idempotence)
4473ebd pending.go: surface pending-reply ledger alongside inbox
89ad2d9 send.go: v0.1.1 extensions (--ask, --reply-to ledger clearing, wake receipt, --json, --no-wake)
b2759fa defer + ledger: address codex review on eb58443 (agent_id boundary + past --until rejection)
82ae6aa gate.go: address codex review on c17e310 (malformed-inbox block + wake_stale warn + missing-reg reason)
eb58443 defer.go: explicit deferral path for pending-reply obligations
675e0bc boot.go: align refreshed semantics with spec rev3 + UX polish
c17e310 gate.go: lifecycle gate for v0.1.1 (consensus/commit/release/finish)
eda3124 boot.go: session-start ritual composing register + pending + ledger
be78332 ledger.go: address codex review on 4d34826 (require reply_message_id, validate archive_path, collision typing, status conservation, task always emitted)
4d34826 ledger.go: pending-reply ledger primitive for v0.1.1
```

Acceptance matrix coverage (spec rev3 Part 4):
- Test 1 (msg-43b6 reproduction): ✅ TestEndToEndMsg43b6Reproduction
- Test 2 (hook safety, no silent drain): ✅ TestPendingIsStrictlyReadOnlyAcrossManyInvocations
- Test 3 (stale-reg block): ✅ TestGateCommitBlocksOnStaleRegAndUnblocksWithAck
- Test 4 (no-tmux fallback): deferred — depends on `watch` (v0.1.2)
- Test 5 (deferral path): ✅ TestDeferTransitionsLedgerAndAcksSender
- Test 6 (three-wrapper hook smoke): operator-level; templates shipped under `examples/hooks/`
- Test 7 (pending truly read-only): ✅ TestPendingIsStrictlyReadOnlyAcrossManyInvocations (50-iter scaled down from 1000)
- Test 8 (boot exit-code matrix): ✅ 5 boot tests
- Test 9 (ledger identity / no collision): ✅ TestRecordPendingReplyDistinguishesByMessageID
- Test 10 (binary-not-on-PATH): deferred — depends on `doctor` (v0.1.2)
- Test 11 (`send --ask` writes frontmatter + body heading): ✅ TestSendAskSetsReplyRequiredAndHeading

Pre-commit ritual (`gofmt -w . && go vet ./... && go test ./... && go build ./...`) green as of `11742d2`. 22 ledger tests + 9 boot + 11 gate + 6 defer + 8 send v0.1.1 + 5 pending v0.1.1 + 3 check-ledger tests.

Spec rev3 §B.1 drift, both real-bake-verified by Alex in a fresh test folder against `claude --dangerously-skip-permissions` (v2.1.145, Opus 4.7), with sign-off from codex + gemini-cli:

1. **Schema shape**: spec shows a flat `{"command": "..."}` per entry. Live Claude Code at code.claude.com/docs/en/hooks.md requires the nested `matcher` + `{type:"command", command:"..."}` shape. Shipped template (b744c31) uses the live schema; codex's `.codex/hooks.json` and gemini's `.gemini/settings.json` already use the nested shape for their own wrappers.

2. **UserPromptSubmit output mode**: spec shows `pending --as claude-code --json`. Real bake confirmed Claude Code's UserPromptSubmit auto-injects plain-text stdout as context but ignores raw JSON without an `additionalContext` field — so `--json` silently fails to inject. Template fix landed in 05e269d (drops `--json`); plain text mode verified working against the live wrapper. The richer Claude-Code-specific JSON output mode (analogous to `--codex-hook UserPromptSubmit`) is deferred to v0.1.2.

Both drifts should be patched in the next spec revision before any new template is bootstrapped from the doc text.

3. **Loop-prevention convention** (AGENTCHUTE.md §6.4.3, 05e269d): Replies with `in_reply_to` set SHOULD default to `reply_required: false`, and automatic acknowledgments MUST NOT set `reply_required: true`. Closes the self-send loop hazard Alex flagged from the Stop-hook real-bake. CLI surface: `send` emits a stderr warning when `--from == --to --ask` to reinforce the convention at the call site.

**Awaiting Alex's approval for:** squash-merge to `main` + `v0.1.1` tag.

## Scope source of truth

Full v0.1.1 implementation spec (rev3, verified by codex + gemini) is at:

- **Local file**: `.agentchute/loop/scratch-v0.1.1-spec.md` (in this repo, NOT committed; lives in the gitignored loop directory)
- The same file was also at `/tmp/agentchute-v0.1.1-spec.md` during the original session. If `/tmp` cleared on reboot, fall back to the `.agentchute/loop/` copy.

Read that spec before writing any v0.1.1 code. The 6-part structure: CLI surface · hook templates · docs changes · bullet-proof defenses · implementation order · acceptance criteria.

## What's done (committed on `v0.1.1` branch)

```
a0d4b2a pending.go: new side-effect-free read for v0.1.1 + tests catch up to v4 enrollment
d7df427 README: extend no-binary quickstart for v0.1.1 lifecycle      (codex)
bd2e2e6 AGENTCHUTE.md: add v0.1.1 reply obligations and wake clarification (codex)
bd286fe README: add Hooks section + v0.1.1 changelog                   (gemini)
93bfe57 examples/hooks/codex: add .codex hooks template for v0.1.1     (codex)
c640df0 enrollment block v3 → v4 across CLAUDE/CODEX/GEMINI/GROK/AGENTS + templates (gemini)
a317491 examples/hooks/gemini: add .gemini/settings.json template      (gemini)
```

All commits are on `v0.1.1`; `main` is unchanged.

Test ritual passes as of `a0d4b2a`:
```sh
gofmt -w .
go vet ./...
go test ./...
go build ./...
```

## What's done per agent

| Agent | Deliverables | Status |
|---|---|---|
| **claude-code** | Go CLI core (ledger, boot, gate, defer, send extensions, pending+check integration), `.claude/settings.json` template, tests | ✅ **complete** (15 commits since `f984c84`; see top of file) |
| **codex** | `.codex/hooks.json`, AGENTCHUTE.md spec edits, README Quickstart hand-protocol, rolling review across every claude-code commit | ✅ **3/3 complete + 5 review rounds** |
| **gemini-cli** | `.gemini/settings.json`, enrollment block rewrites, README Hooks section + v0.1.1 changelog, AGENTCHUTE.md §6.4.1 `deferred-reply` task entry, rolling review | ✅ **complete + final docs pass** |
| **grok** | observer (CLI changing soon) | — |

## Deferred to v0.1.2 (Phase 3 polish)

These were on the original queue but explicitly scoped out of the v0.1.1 ship per the handoff:

- **`agentchute doctor`** — diagnostic aggregator (registration freshness, inbox state, wake target validity, binary on PATH, hook content sanity). Spec rev3 §A.7 + Failure 5 + Failure 10. Test 10 from the acceptance matrix depends on this.
- **`agentchute watch`** with `--notify`, `--print`, `--exec <cmd>`. Recipient-side persistent watcher + OS notification wake adapter (`wake_method: macos-notification` / `linux-notify-send`). Spec rev3 §A.6. Test 4 from the acceptance matrix depends on this.
- **Three-wrapper hook smoke test** (Test 6) — operator-level end-to-end verification. The three templates are committed under `examples/hooks/` and the spec rev3 §B verification was done by codex (codex-cli 0.131.0) and gemini-cli (0.42.0) during their reviews. A scripted in-suite smoke test is out of scope for v0.1.1.

Anything else surfaced by Alex's review of `v0.1.1` lands either as fixup commits on this branch (pre-merge) or as v0.1.2 work (post-tag).

## Resume protocol for any agent

When resuming in a new session:

1. **Re-register**: `agentchute register --as <id> --vendor <vendor>` (or rebuild from source via `env -u GOROOT /opt/homebrew/bin/go build -o /tmp/agentchute .` if binary missing).
2. **Pull latest**: `git pull origin v0.1.1`
3. **Check this file** (`V0.1.1-HANDOFF.md`) for current state.
4. **Check inbox**: `agentchute check --as <id>`
5. **Read the spec**: `.agentchute/loop/scratch-v0.1.1-spec.md` if doing implementation work.

## Constraints (preserved from session)

- Do NOT push to `main` without explicit approval. Commits land on `v0.1.1` branch only.
- Do NOT tag a release without explicit approval.
- Standard pre-commit ritual: `gofmt -w . && go vet ./... && go test ./... && go build ./...`. All four must pass.
- The agentchute binary may not be at `~/.local/bin/agentchute` on resume. If missing, rebuild from source at the repo root and stash at `/tmp/agentchute` or `./agentchute`.

## After v0.1.1 is complete

When all of claude-code's queue is done:
1. Verify all 11 acceptance criteria pass.
2. Squash-merge `v0.1.1` → `main` (Alex's approval).
3. Tag `v0.1.1` (Alex's approval).
4. Cloudflare auto-deploys site changes on push to main.
5. Update README install instructions if the binary path / install-script behavior changed.

— claude-code, session pause 2026-05-19
